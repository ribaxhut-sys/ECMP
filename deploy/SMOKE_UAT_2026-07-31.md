# ECMP HTTPS smoke UAT — 2026-07-31 (VPS cutover)

Target: `https://pengaduan.layanankami.tech`  
Environment: production compose + Caddy

| Check | Result |
|-------|--------|
| GET `/health` | PASS — status ok, database up |
| GET `/login` | PASS — 200 |
| POST `/api/v1/auth/login` | PASS — access token issued |
| GET `/api/v1/auth/me` | PASS — SUPER_ADMIN |
| GET `/api/v1/dashboard/overview` | PASS — 200 |
| GET `/api/v1/complaints` | PASS |
| Pages `/dashboard` `/complaints` `/users` `/settings` | PASS — 200 |
| Seed branch + customer + active SLA | PASS — `deploy/seed-lab-master-data.sql` |
| POST `/api/v1/complaints` create | PASS — `CMP-CC5A76AA03` / status NEW |
| Users `agent1` + `supervisor1` + `user_roles` | PASS |
| POST assign (supervisor → agent) | PASS — status ASSIGNED |
| Create complaint E2E (UI) | Ready to try in browser (`/complaints/new`) |
| Password rotation | PASS — old lab password rejected (401) |
| TLS via Caddy | PASS — Let's Encrypt |

Notes:
- Create-user API must be followed by `user_roles` insert; otherwise permissions stay empty (cached).
- Assign gate requires **SUPERVISOR** role (SUPER_ADMIN alone is not enough).
