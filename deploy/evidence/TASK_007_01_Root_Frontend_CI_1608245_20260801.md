# TASK-007-01 — Root Frontend CI + check:auth-routes (SHA-bound)

| Field | Value |
|---|---|
| Document ID | EVD-TASK-007-01-FE-CI-001 |
| Task | TASK-007-01 |
| Date (UTC) | 2026-08-01T04:25:47Z |
| Branch | `feature/cm-batch1-s2-persistence` |
| Full SHA | `16082454659d7f511e5296d0bd9531185766e6db` |
| Short SHA | `1608245` |
| Workflow mirror | `.github/workflows/root-frontend-ci.yml` (canonical `frontend/`) |
| Node | v20.19.4 (matches CI pin major 20) |
| Working directory | `frontend/` |
| Env | `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000` |
| Source / test / API / DB / ADR / DEC changes | **None** |
| Release tag / RC claim / signatures | **None** |
| Mode B | **CLOSED** (enterprise guard self-test only) |

---

## 1. Repository verification

| Check | Result |
|---|---|
| Branch = `feature/cm-batch1-s2-persistence` | **PASS** |
| HEAD = `16082454659d7f511e5296d0bd9531185766e6db` | **PASS** |
| Abbreviated `1608245` | **PASS** |

---

## 2. Commands executed

Install (prerequisite for local mirror of CI):

```text
npm ci
```

Steps mirrored from `root-frontend-ci.yml` (in order):

| # | Step | Command | Exit | Hard / warn |
|---|---|---|---|---|
| 1 | Dependency audit (high+) | `npm audit --audit-level=high` | **1** | Warn (`continue-on-error: true` in workflow) |
| 2 | Type check | `npm run typecheck` | **0** | Hard |
| 3 | Lint | `npm run lint` | **0** | Hard |
| 4 | Mode A credential route inventory | `ECMP_FRONTEND_DEPLOY_MODE=standalone npm run check:auth-routes` | **0** | Hard |
| 5 | Enterprise guard self-test | `npm run check:auth-routes:enterprise-guard` | **0** | Hard |
| 6 | Auth-route unit tests | `npm run check:auth-routes:unit` | **0** | Hard |
| 7 | Unit tests + coverage | `npm run test:coverage` | **0** | Hard |
| 8 | Accessibility smoke | `npm run test:a11y` | **0** | Warn |
| 9 | Production build | `npm run build` | **0** | Hard |

Full stdout/stderr log: `deploy/evidence/TASK_007_01_Root_Frontend_CI_log_1608245_20260801.txt`

---

## 3. check:auth-routes summary

**Standalone inventory (exit 0):**

```text
Mode A credential routes (FE-ARCH §5.3 / ADR-014):
  [PRESENT] src/app/login/page.tsx
  [PRESENT] src/app/forgot-password/page.tsx
  [PRESENT] src/app/reset-password/page.tsx
  [PRESENT] src/app/change-password/page.tsx
  [PRESENT] src/app/(app)/profile/security/change-password/page.tsx
OK (standalone): 5/5 Mode A credential route(s) present — allowed for Mode A delivery.
```

**Enterprise guard self-test (exit 0):** process correctly hard-fails under enterprise profile while routes remain → `SELF-TEST OK`. Does **not** unlock Mode B.

**Unit tests:** `check:auth-routes:unit` exit 0.

---

## 4. Hard-gate summary

| Gate | Result |
|---|---|
| typecheck | PASS |
| lint | PASS |
| check:auth-routes (+ enterprise-guard + unit) | PASS |
| test:coverage | PASS — 18 files; All files stmts/lines **88.55%**, branches **79.53%**, funcs **94.87%** |
| build | PASS |
| npm audit (warn) | Exit 1 — known warn posture; not hard-fail per workflow |
| test:a11y (warn) | PASS (exit 0) |

**Task hard-path verdict:** all hard steps green at SHA `1608245`.

---

## 5. Explicit non-claims

- Not READY FOR RC  
- Not Production / Enterprise Ready  
- No annotated tag created  
- No human signatures or approvals invented  
- Mode B remains CLOSED  

---

## 6. Observations

1. `npm audit --audit-level=high` exited **1** (vulnerabilities present); workflow treats as warn — recorded, not “fixed” (out of TASK-007-01 scope).  
2. Local `frontend/node_modules` populated by `npm ci` for this run; **not** an evidence commit target.  
3. Workspace may have unrelated dirty docs from prior TASK-006-01; this task only added files under `deploy/evidence/`.

---

*End of EVD-TASK-007-01-FE-CI-001*
