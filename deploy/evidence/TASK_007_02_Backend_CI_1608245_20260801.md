# TASK-007-02 — Backend CI (SHA-bound)

| Field | Value |
|---|---|
| Document ID | EVD-TASK-007-02-BE-CI-001 |
| Task | TASK-007-02 |
| Date (UTC) | 2026-08-01T04:29:22Z (start) / 2026-08-01T04:32:30Z (approx end) |
| Branch | `feature/cm-batch1-s2-persistence` |
| Full SHA | `16082454659d7f511e5296d0bd9531185766e6db` |
| Short SHA | `1608245` |
| Workflow | `.github/workflows/backend-ci.yml` |
| Runtime mirror | `python:3.13-slim` + isolated `postgres:16-alpine` (`ecmp:ecmp_ci@127.0.0.1:55432/ecmp_test`) |
| Working directory | `backend/` (inside ephemeral workspace copy; host `backend/` not written) |
| Env | `DATABASE_URL=postgresql+psycopg://ecmp:ecmp_ci@127.0.0.1:55432/ecmp_test` |
| Source / runtime / API / DB schema-in-repo / ADR / DEC changes | **None** |
| Release tag / RC claim / signatures / Mode B | **None** |

---

## 1. Repository verification

| Check | Result |
|---|---|
| Branch = `feature/cm-batch1-s2-persistence` | **PASS** |
| HEAD = `16082454659d7f511e5296d0bd9531185766e6db` | **PASS** |
| Abbreviated `1608245` | **PASS** |
| Active workflow `.github/workflows/backend-ci.yml` present | **PASS** |

---

## 2. Workflow executed

Local mirror of **only** `.github/workflows/backend-ci.yml` jobs:

### Job `test`

| # | Step | Command | Exit |
|---|---|---|---|
| 1 | Install dependencies | `python -m pip install --upgrade pip && pip install -r requirements-dev.txt` | **0** |
| 2 | Show Ruff config source | `python -c …` + `ruff check --show-settings app \| head -n 40` | **0** |
| 3 | Lint backend (ruff) | `ruff check app tests` | **0** |
| 4 | Validate OpenAPI contracts | validate all `07 API Catalog/openapi/**/*.yaml` | **0** |
| 5 | Run migrations (Alembic) | `alembic upgrade head` | **0** |
| 6 | Tests + coverage gate | `pytest -q --cov=app --cov-report=term-missing --cov-fail-under=90` | **0** |

### Job `security-audit`

| # | Step | Command | Exit |
|---|---|---|---|
| 7 | Audit runtime dependencies | `pip-audit -r requirements.txt` | **0** |

**Overall process exit (all steps):** **0**

Full stdout/stderr: `deploy/evidence/TASK_007_02_Backend_CI_log_1608245_20260801.txt`

---

## 3. Execution result summary

| Gate | Result |
|---|---|
| Ruff lint (`app` + `tests`) | **PASS** — `All checks passed!` |
| OpenAPI validation | **PASS** — 9 specs OK |
| Alembic upgrade head | **PASS** |
| Pytest | **PASS** — `1325 passed, 7 skipped` (exit 0) |
| Coverage gate (process exit) | **PASS (exit 0)** — see Observation A |
| pip-audit | **PASS** — `No known vulnerabilities found` |

---

## 4. Observations

### A — Coverage message vs process exit (matches GHA)

Pytest printed:

```text
TOTAL ... 90%
FAIL Required test coverage of 90% not reached. Total coverage: 89.86%
1325 passed, 7 skipped, ...
```

Process exit code remained **0** (verified twice in mirror; same message appears on GitHub Actions Backend CI run `30682105072` @ ancestor `a5391d6`, which concluded **success**). Backend tree at tip is unchanged vs that green run (`git diff a5391d6..1608245 -- backend/ "07 API Catalog/openapi/" .github/workflows/backend-ci.yml` empty).

**No code was changed** to “fix” coverage (out of TASK-007-02 scope / constraints).

### B — Diagnostic show-settings quoting

Local harness quote-escaping caused a transient `NameError` on the `python -c` path-print line inside step 2; `ruff check --show-settings` still resolved `backend/pyproject.toml`, and the **blocking** lint step (3) passed. Does not affect hard-gate verdict.

### C — Host isolation

Repo mounted read-only into the runner; CI ran on `/workspace` copy. Dedicated Postgres `ecmp-ci-pg-task00702` used and removed after the run. Lab stack (`ecmp-postgres` / `ecmp-backend`) left running.

---

## 5. Explicit non-claims

- Not READY FOR RC  
- Not Production / Enterprise Ready  
- No annotated tag created  
- No human signatures or approvals invented  
- Mode B remains CLOSED  
- TASK-007-03 (G2 re-bind @ freeze) **not** executed  

---

## 6. Cross-reference

| Item | Link |
|---|---|
| Prior FE tip attestation | `TASK_007_01_Root_Frontend_CI_1608245_20260801.md` |
| REL-RC §2 gap addressed (Backend CI tip-bound) | This document + log |
| Prior GHA Backend CI (ancestor, same `backend/` tree) | https://github.com/ribaxhut-sys/ECMP/actions/runs/30682105072 @ `a5391d6` |

---

*End of EVD-TASK-007-02-BE-CI-001*
