# Dependencies / build
node_modules/
.next/
out/
dist/
build/
tmp/
*.egg-info/
.venv/
venv/

# Python
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.pyc
.coverage
coverage/
htmlcov/

# Env & secrets
.env
.env.local
.env.*.local

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp

# Databases / local artifacts
*.db
*.sqlite
*.sqlite3

# Coverage / local probe dumps
*_cov_*.txt

# Docs site
site/

# Frontend coverage / telemetry noise
implementation/frontend/coverage/
implementation/frontend/dist/
*.tsbuildinfo

# AI platform local orchestrator telemetry
ai-platform/telemetry/runs/route_*.json

# keep metrics history tracked; ignore local venv only

# Local go-live artifacts
backups/
.env
.env.*
!.env.example
!.env.production.example

# Local Compose secret injection (SIT / shared activation — never commit)
docker-compose.override.yml

# Local attachment blobs (TASK-029 LocalStorageProvider)
backend/data/attachments/
backend/storage/

# Lab credential artifacts — never commit
ecmp-lab-users-temp-passwords.xlsx
deploy/ecmp-lab-users-temp-passwords.xlsx

# Local S3 migrate/smoke artifacts (dumps, reports)
backend/_s3_migrate_artifacts/
backend/_s3_migrate_live.py
backend/_s3_smoke_live.py
backend/_tmp_*.py
backend/_tmp_*.txt

