[pytest]
testpaths = tests
pythonpath = .
# DB target is forced to ecmp_test (or *_test / sqlite) by tests/conftest.py —
# never the shared lab/public ``ecmp`` database.
filterwarnings =
    ignore::DeprecationWarning
markers =
    security: Foundation security verification suite (SECMIG-P5-006)
