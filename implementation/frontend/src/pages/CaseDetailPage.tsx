/**
 * Case Detail route shell — workspace implementation lands in Step 2.
 * Kept as a thin page so the router is buildable after Step 0.
 */
export function CaseDetailPage() {
  return (
    <p style={{ padding: '1.5rem', fontFamily: 'system-ui, sans-serif' }}>
      Case Detail Workspace (Step 2)
    </p>
  )
}
